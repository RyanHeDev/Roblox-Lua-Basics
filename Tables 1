

-- Tables

-- What you cam do without tables
CarBrand = "Lamborghini"
carColor = "Black"
CarTopSpeed = 1000
print(CarBrand, carColor, CarTopSpeed)
-- But instead you can use tables and do this
carDetails = {"Lamborghini","Black",1000}
table.remove(carDetails, 3)
print(carDetails)
-- To add something at the end of the table we use table.
-- First add a table then add the data type you want to add
table.insert(carDetails, 2000)
print(carDetails[3])
-- Credit : Tab for documenting
